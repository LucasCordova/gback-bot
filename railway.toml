# Railway config — always-on Discord bot (replaces cron approach)
[build]
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python bot.py"
# No cron schedule — this is a long-running process
